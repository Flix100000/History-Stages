# gradle
build
.gradle

# dev run directories
run
run-data
run-server
run-gametest
# Forge runs get their own directories: the working copy is shared with the neoforge branch,
# and a 1.20.1 server must never open a 1.21 world or config.
run-server-forge
run-gametest-forge

# eclipse / vscode
bin
.eclipse/
*.launch
.settings
.metadata
.classpath
.project
.vscode/

# idea
out
*.ipr
*.iws
*.iml
# keep everything out of .idea except the project icon
.idea/*
!.idea/icon.png

# Files from Forge MDK
forge*changelog.txt

# Mods pulled in only to reproduce a bug report. build.gradle applies this file when it is here,
# so the build server never sees it. Restore it from docs/forge-dev-setup/.
dev-deps.gradle

# local notes
docs/
.superpowers/
.claude/
CLAUDE.md
