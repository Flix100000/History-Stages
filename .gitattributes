# Line endings are stored as LF no matter what a contributor's core.autocrlf says.
* text=auto eol=lf

# cmd.exe misreads batch files with LF endings.
*.bat text eol=crlf

*.jar binary
*.png binary
*.nbt binary
