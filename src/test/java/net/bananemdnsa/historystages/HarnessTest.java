package net.bananemdnsa.historystages;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

/** Proves the JUnit harness is wired up. Delete once real tests exist if it feels redundant. */
class HarnessTest {

    @Test
    void harnessRuns() {
        assertEquals(2, 1 + 1);
    }
}
